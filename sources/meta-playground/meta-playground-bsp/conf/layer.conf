# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-playground-bsp"
BBFILE_PATTERN_meta-playground-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-playground-bsp = "10"

LAYERDEPENDS_meta-playground-bsp = "core"
LAYERSERIES_COMPAT_meta-playground-bsp = "walnascar"
